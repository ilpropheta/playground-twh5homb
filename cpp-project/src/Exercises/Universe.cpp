// { autofold
#include "Universe.h"
// }

int Universe::CountAllStars(vector<int>& galaxies) {
    int total_stars = 0;
    for(int stars : galaxies) {
        total_stars = stars; // fix me!
    }
    return total_stars;
}

